name: Playwright E2E Tests

on:
  push:
    branches: [ main ]
    paths:
      - 'website-integration/ArrowheadSolution/**'
      - 'website-integration/ArrowheadSolution/tests/e2e/**'
      - 'website-integration/ArrowheadSolution/playwright.config.ts'
      - '.github/workflows/e2e-playwright.yml'
  pull_request:
  workflow_dispatch:
    inputs:
      browsers:
        description: 'chromium | firefox | webkit | all'
        required: false
        default: 'all'
  schedule:
    - cron: '0 2 * * *' # nightly at 02:00 UTC

concurrency:
  group: e2e-playwright-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    name: Run Playwright E2E (chromium)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'website-integration/ArrowheadSolution/package-lock.json'

      - name: Install dependencies
        run: npm ci
        working-directory: website-integration/ArrowheadSolution

      - name: Install Playwright Browsers
        run: npm run test:install
        working-directory: website-integration/ArrowheadSolution

      - name: Run E2E tests (chromium)
        run: npx playwright test --project=chromium
        working-directory: website-integration/ArrowheadSolution
        env:
          CI: true
          # Supabase credentials for test database operations
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          # Ensure Playwright starts the webServer defined in config
          PLAYWRIGHT_NO_WEBSERVER: ''
          # Explicitly skip admin tests in CI until test DB is wired
          E2E_SKIP_ADMIN: '1'
          # Skip billing tests in CI until Stripe secrets are configured
          E2E_SKIP_BILLING: '1'
          ADMIN_SESSION_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          ADMIN_COOKIE_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          FLASK_DEBUG: '1'

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: website-integration/ArrowheadSolution/playwright-report
          if-no-files-found: warn
          retention-days: 14

      - name: Upload E2E test results (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results
          path: website-integration/ArrowheadSolution/test-results
          if-no-files-found: ignore
          retention-days: 7

  e2e-nightly:
    if: github.event_name == 'schedule'
    name: Nightly Playwright E2E (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox, webkit]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'website-integration/ArrowheadSolution/package-lock.json'

      - name: Install dependencies
        run: npm ci
        working-directory: website-integration/ArrowheadSolution

      - name: Install Playwright Browsers
        run: npm run test:install
        working-directory: website-integration/ArrowheadSolution

      - name: Run E2E tests (${{ matrix.browser }})
        run: npx playwright test --project=${{ matrix.browser }}
        working-directory: website-integration/ArrowheadSolution
        env:
          CI: true
          # Supabase credentials for test database operations
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PLAYWRIGHT_NO_WEBSERVER: ''
          E2E_SKIP_ADMIN: '1'
          E2E_SKIP_BILLING: '1'
          ADMIN_SESSION_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          ADMIN_COOKIE_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          FLASK_DEBUG: '1'

      - name: Upload Playwright report (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}
          path: website-integration/ArrowheadSolution/playwright-report
          if-no-files-found: warn
          retention-days: 7

      - name: Upload E2E test results (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results-${{ matrix.browser }}
          path: website-integration/ArrowheadSolution/test-results
          if-no-files-found: ignore
          retention-days: 7

  e2e-webkit-pr:
    if: github.event_name == 'pull_request'
    name: Run Playwright E2E (webkit, PR)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'website-integration/ArrowheadSolution/package-lock.json'

      - name: Install dependencies
        run: npm ci
        working-directory: website-integration/ArrowheadSolution

      - name: Install Playwright Browsers
        run: npm run test:install
        working-directory: website-integration/ArrowheadSolution

      - name: Run E2E tests (webkit)
        run: npx playwright test --project=webkit
        working-directory: website-integration/ArrowheadSolution
        env:
          CI: true
          # Supabase credentials for test database operations
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PLAYWRIGHT_NO_WEBSERVER: ''
          E2E_SKIP_ADMIN: '1'
          E2E_SKIP_BILLING: '1'
          ADMIN_SESSION_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          ADMIN_COOKIE_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          FLASK_DEBUG: '1'

      - name: Upload Playwright report (webkit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-webkit
          path: website-integration/ArrowheadSolution/playwright-report
          if-no-files-found: warn
          retention-days: 7

      - name: Upload E2E test results (webkit)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results-webkit
          path: website-integration/ArrowheadSolution/test-results
          if-no-files-found: ignore
          retention-days: 7

  e2e-dispatch:
    if: github.event_name == 'workflow_dispatch'
    name: Run Playwright E2E (${{ matrix.browser }}, dispatch)
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        browser: ${{ fromJSON( github.event.inputs.browsers && (github.event.inputs.browsers == 'all' && '["chromium","firefox","webkit"]' || format('["{0}"]', github.event.inputs.browsers)) || '["chromium","firefox","webkit"]' ) }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: 'website-integration/ArrowheadSolution/package-lock.json'

      - name: Install dependencies
        run: npm ci
        working-directory: website-integration/ArrowheadSolution

      - name: Install Playwright Browsers
        run: npm run test:install
        working-directory: website-integration/ArrowheadSolution

      - name: Run E2E tests (${{ matrix.browser }})
        run: npx playwright test --project=${{ matrix.browser }}
        working-directory: website-integration/ArrowheadSolution
        env:
          CI: true
          # Supabase credentials for test database operations
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ secrets.SUPABASE_JWT_SECRET }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          PLAYWRIGHT_NO_WEBSERVER: ''
          E2E_SKIP_ADMIN: '1'
          E2E_SKIP_BILLING: '1'
          ADMIN_SESSION_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          ADMIN_COOKIE_SECRET: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          FLASK_DEBUG: '1'

      - name: Upload Playwright report (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.browser }}
          path: website-integration/ArrowheadSolution/playwright-report
          if-no-files-found: warn
          retention-days: 7

      - name: Upload E2E test results (${{ matrix.browser }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-test-results-${{ matrix.browser }}
          path: website-integration/ArrowheadSolution/test-results
          if-no-files-found: ignore
          retention-days: 7
